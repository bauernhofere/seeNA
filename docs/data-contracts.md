# Scientific contracts

The authoritative methods/contracts document is [inst/methods.md](../inst/methods.md).
It ships with the installed package and is accessible with:

```r
file.show(system.file("methods.md", package = "ichorViz"))
```

Keep one authoritative copy; do not duplicate the scientific rules here.
